.idea
.vscode
*.code-workspace
*.pyc
__pycache__
*~

db.sqlite3
celerybeat-schedule
matorral/localsettings.py
matorral/local.py

_dist/
.env
.env.production
.venv
venv
.DS_Store

media/
static/
# Brand images are source assets, not build output: keep them tracked so they
# reach deployments. Everything else under static/ is generated by Vite.
!static/images/
!static/images/**
static_root/
/.vite/
node_modules/
.sass-cache/

.playwright-browsers/

.coverage
coverage.*

.claude/settings.local.json
openapitools.json

scripts/
fly.toml

# local (non-Docker) setup artifacts
matorral.egg-info/

# Local (non-Docker) environment pins — not used by the Docker/production build,
# which installs from pyproject.toml.
requirements-local.txt
